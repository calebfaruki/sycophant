apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: vap-rejects-workspace-projected-token
spec:
  description: |
    HIGHEST BLAST RADIUS. The workspace VAP rejects pods declaring a
    projected SA token volume. If a workspace could mint kube-apiserver
    bearer tokens, it could read its own SA's RBAC and escalate. This
    rule is the only structural defender.
  bindings:
    - name: tenant_ns
      value: tenant-chainsaw-wps-proj-tok
    - name: target_namespace
      value: tenant-chainsaw-wps-proj-tok
  steps:
    - name: create-tenant-ns-via-deployer
      try:
        - script:
            env:
              - name: TENANT_NS
                value: ($tenant_ns)
            content: |
              kubectl create namespace "$TENANT_NS" --as=system:serviceaccount:infra:tenant-deployer
    - name: assert-sycophant-mutate-stamped-labels
      try:
        - assert:
            resource:
              apiVersion: v1
              kind: Namespace
              metadata:
                name: ($tenant_ns)
                labels:
                  app.kubernetes.io/part-of: sycophant-tenant
                  pod-security.kubernetes.io/enforce: restricted
    - name: assert-sycophant-generated-vapbinding
      try:
        - assert:
            resource:
              apiVersion: admissionregistration.k8s.io/v1
              kind: ValidatingAdmissionPolicyBinding
              metadata:
                name: (join('-', [$tenant_ns, 'workspace-pod-binding']))
    - name: wait-for-vap-cache-refresh
      try:
        - sleep:
            duration: 5s
    - name: apply-violating-workspace-pod
      try:
        - apply:
            file: ../../fixtures/workspace-pod-with-projected-token.yaml
            template: true
            expect:
              - check:
                  ($error != null): true
                  (contains($error, 'Projected volume sources must be')): true
      catch:
        - events:
            namespace: ($tenant_ns)
      finally:
        - script:
            env:
              - name: TENANT_NS
                value: ($tenant_ns)
            content: |
              kubectl delete namespace "$TENANT_NS" --ignore-not-found --wait=false
