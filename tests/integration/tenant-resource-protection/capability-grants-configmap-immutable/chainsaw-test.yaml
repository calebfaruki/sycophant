apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: protect-capability-grants-configmap-blocks-same-ns
spec:
  description: |
    cluster-protect-security must deny CREATE, UPDATE, and DELETE on any
    ConfigMap labeled sycophant.md/type=capability-grants from any principal
    whose ServiceAccount lives inside the tenant namespace. This is the rendered
    secret-name allowlist the downstream admission gate reads; a workspace that
    could write it would mint itself approval for any Secret and the gate would
    then be checking a table the tenant wrote.

    The twin of protect-relay-toolset-grants / protect-relay-access-grants: same
    match shape, same same-namespace precondition, keyed on the exact
    sycophant.md/type label. The
    protection is keyed on that label, so the new capability-grants type needs
    its own rule -- RBAC absence is the first lock, this is the second.

    RBAC in this test is deliberately permissive (a Role granting the attacker SA
    full CRUD on ConfigMaps) so the request reaches admission and Kyverno is the
    rule under test rather than RBAC.

    Live-cluster test, justified: rejection at admission is only observable
    in-cluster. A render assertion on the ClusterPolicy
    proves the deny rule is declared but not that the apiserver denies the write;
    only an in-cluster attempt observes the rejection. A rendered-RBAC assertion
    is not load-bearing here because tenant workspace SAs hold no verbs on any
    resource regardless of this ConfigMap, so it would not go red on a
    ConfigMap-specific mutation. The identical sibling property (the
    relay-toolset-grants and relay-access-grants ConfigMaps) is already tested live in this
    bucket.

    Mutation guard: without the new capability-grants rule in
    charts/sycophant-cluster/templates/kyverno-cpol.yaml all three deny steps go
    green (admitted).
  bindings:
    - name: tenant_ns
      value: (join('-', ['tenant', random('[a-z0-9]{8}')]))
  steps:
    - name: create-tenant-ns
      use:
        template: ../../step-templates/tenant.yaml
        with:
          bindings:
            - name: tenant_ns
              value: ($tenant_ns)
    - name: seed-attacker-principal
      use:
        template: ../../step-templates/attacker-principal.yaml
        with:
          bindings:
            - name: tenant_ns
              value: ($tenant_ns)
            - name: principal_name
              value: attacker
            - name: rules
              value:
                - apiGroups: [""]
                  resources: ["configmaps"]
                  verbs: ["create", "update", "patch", "delete", "get", "list"]
    - name: operator-seeds-the-configmap
      try:
        - apply:
            template: true
            resource:
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: capability-grants-research
                namespace: ($tenant_ns)
                labels:
                  app.kubernetes.io/part-of: sycophant
                  sycophant.md/type: capability-grants
              data:
                research-notion-reader: notion
        - assert:
            resource:
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: capability-grants-research
                namespace: ($tenant_ns)
    - name: in-namespace-sa-cannot-update-the-configmap
      try:
        - script:
            env:
              - name: TENANT_NS
                value: ($tenant_ns)
            content: |
              kubectl patch configmap capability-grants-research -n "$TENANT_NS" --type=merge \
                -p '{"data":{"attacker-secret":"notion"}}' \
                --as="system:serviceaccount:${TENANT_NS}:attacker"
            check:
              ($error != null): true
              (contains($stderr, 'cluster-protect-security')): true
    - name: in-namespace-sa-cannot-delete-the-configmap
      try:
        - script:
            env:
              - name: TENANT_NS
                value: ($tenant_ns)
            content: |
              kubectl delete configmap capability-grants-research -n "$TENANT_NS" \
                --as="system:serviceaccount:${TENANT_NS}:attacker"
            check:
              ($error != null): true
              (contains($stderr, 'cluster-protect-security')): true
    - name: in-namespace-sa-cannot-create-a-shadow-configmap
      try:
        - script:
            env:
              - name: TENANT_NS
                value: ($tenant_ns)
            content: |
              cat <<MANIFEST | kubectl apply --as="system:serviceaccount:${TENANT_NS}:attacker" -n "$TENANT_NS" -f -
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: capability-grants-shadow
                labels:
                  sycophant.md/type: capability-grants
              data:
                attacker-secret: notion
              MANIFEST
            check:
              ($error != null): true
              (contains($stderr, 'cluster-protect-security')): true
      catch:
        - events:
            namespace: ($tenant_ns)
