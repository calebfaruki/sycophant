apiVersion: v1
kind: Pod
metadata:
  name: transponder-good
  namespace: ($target_namespace)
  labels:
    app.kubernetes.io/part-of: sycophant
    app.kubernetes.io/component: transponder
spec:
  # No runtimeClassName: the transponder runs on the kubelet-default
  # runtime (runc). gVisor is stamped/required on chambers (airlock-job)
  # only -- the mutate must NOT add it here and the VAP must NOT require it.
  # Suppresses kubelet's default kube-apiserver-audience token mount;
  # the projected volume below provides a custom-audience token instead.
  # Mirrors the chart-rendered transponder Deployment.
  automountServiceAccountToken: false
  volumes:
    - name: transponder-auth
      projected:
        sources:
          - serviceAccountToken:
              path: token
              audience: transponder.hangar.sycophant.md
              expirationSeconds: 3600
  containers:
    - name: transponder
      image: registry.k8s.io/pause:3.9
      volumeMounts:
        - name: transponder-auth
          mountPath: /var/run/secrets/kubernetes.io/serviceaccount
          readOnly: true
      resources:
        requests: { cpu: "100m", memory: "64Mi" }
        limits:   { cpu: "100m", memory: "64Mi" }
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 1000
        capabilities:
          drop: ["ALL"]
        seccompProfile:
          type: RuntimeDefault
  terminationGracePeriodSeconds: 0
