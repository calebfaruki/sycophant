apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: off-allowlist-volume-kind-denies
spec:
  description: |
    A Job created under `harness-<ws>` whose pod template declares a volume of a
    kind outside the six-kind safe set (configMap, secret, projected, emptyDir,
    downwardAPI, the workspace persistentVolumeClaim) is denied. The creator
    submits a `csi` volume -- the secrets-store side channel the volume-kind
    allowlist exists to close.

    Well-formed in every other respect, so the only reason to deny is the
    volume-kind allowlist. With that rule removed the Job admits and this test
    goes red.

    Live-cluster test: the gate keys on the creating identity.

    Mutation guard: removing the volume-kind allowlist admits this Job.
  bindings:
    - name: tenant_ns
      value: (join('-', ['tenant', random('[a-z0-9]{8}')]))
  steps:
    - name: create-tenant-ns
      use:
        template: ../../step-templates/tenant.yaml
        with:
          bindings:
            - name: tenant_ns
              value: ($tenant_ns)
    - name: seed-capability-grants
      use:
        template: ../../step-templates/capability-grants.yaml
        with:
          bindings:
            - name: tenant_ns
              value: ($tenant_ns)
            - name: grants
              value:
                approved-secret: tool
    - name: harness-creates-csi-volume-job
      try:
        - script:
            env:
              - name: TENANT_NS
                value: ($tenant_ns)
            content: |
              cat <<MANIFEST | kubectl create --as="system:serviceaccount:${TENANT_NS}:harness-probe" -n "$TENANT_NS" -f -
              apiVersion: batch/v1
              kind: Job
              metadata:
                name: off-volume-kind
              spec:
                template:
                  metadata:
                    labels:
                      sycophant.md/job-kind: tool
                  spec:
                    restartPolicy: Never
                    serviceAccountName: unprivileged-probe
                    volumes:
                      - name: side-channel
                        csi:
                          driver: secrets-store.csi.k8s.io
                          readOnly: true
                    containers:
                      - name: probe
                        image: registry.k8s.io/pause:3.9
              MANIFEST
            check:
              ($error != null): true
              (contains($stderr, 'cluster-capability-job-gate')): true
      catch:
        - events:
            namespace: ($tenant_ns)
