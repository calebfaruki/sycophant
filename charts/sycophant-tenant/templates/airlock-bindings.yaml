apiVersion: v1
kind: ConfigMap
metadata:
  name: airlock-bindings
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sycophant.labels" . | nindent 4 }}
    sycophant.md/type: airlock-bindings
data:
  bindings.yaml: |
    {{- range $name, $ws := .Values.workspaces }}
    {{ $name }}:
      {{- if hasKey $ws "chambers" }}
      {{- range $chamber := $ws.chambers }}
      - {{ $chamber }}
      {{- end }}
      {{- else }}
      - stdlib
      {{- end }}
    {{- end }}
