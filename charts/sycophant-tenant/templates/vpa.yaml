{{- if .Values.vpa.enabled }}
# Vertical Pod Autoscaler for the tenant controller singletons. VPA is
# optional operational infrastructure — operators who
# skip it pay for unused reserved capacity; nothing breaks.
#
# Requires VPA 1.2+ (InPlaceOrRecreate update mode) and Kubernetes 1.35+
# (in-place pod resize GA). With `resizePolicy: NotRequired` set on each
# controller's container, an in-place memory or CPU adjustment preserves
# streaming connections (harness Subscribe, channel ChannelStream)
# instead of recreating the pod.
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: toolset-ctrl
  labels:
    {{- include "sycophant.labels" . | nindent 4 }}
    app.kubernetes.io/component: toolset-ctrl
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: toolset-ctrl
  updatePolicy:
    updateMode: InPlaceOrRecreate
  resourcePolicy:
    containerPolicies:
      - containerName: '*'
        minAllowed:
          cpu: {{ .Values.toolsetCtrl.vpa.minAllowed.cpu | quote }}
          memory: {{ .Values.toolsetCtrl.vpa.minAllowed.memory | quote }}
        maxAllowed:
          cpu: {{ .Values.toolsetCtrl.vpa.maxAllowed.cpu | quote }}
          memory: {{ .Values.toolsetCtrl.vpa.maxAllowed.memory | quote }}
{{- end }}
