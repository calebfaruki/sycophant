{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["policyEngine", "idp"],
  "properties": {
    "global": { "type": "object" },
    "policyEngine": {
      "description": "Admission-policy engine: 'kyverno' renders the framework ClusterPolicies, 'external' renders none. No default — you must declare one.",
      "enum": ["kyverno", "external"]
    },
    "idp": {
      "type": "object",
      "additionalProperties": false,
      "description": "The IdP trust block rendered into the apiserver AuthenticationConfiguration. The chart always renders StructuredAuthenticationConfiguration from it.",
      "required": ["issuer", "audience"],
      "properties": {
        "issuer": {
          "type": "string",
          "format": "uri",
          "minLength": 1,
          "description": "OIDC issuer URL. The apiserver fetches JWKS from this issuer's Discovery endpoint at runtime."
        },
        "audience": {
          "type": "string",
          "minLength": 1,
          "description": "Mandatory aud value. A trust block without an audience is invalid."
        },
        "certificateAuthority": {
          "type": "string",
          "minLength": 1,
          "description": "Optional PEM CA bundle the apiserver trusts when fetching this issuer's Discovery/JWKS over TLS. Omit for a publicly-trusted issuer (Okta, Entra, Google); the system trust store is used. Required only for a self-hosted or bundled issuer whose cert does not chain to a public root, so trusting it stays config, not an apiserver trust-store change."
        }
      }
    },
    "deployer": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "serviceAccount": {
          "type": "object",
          "additionalProperties": false,
          "required": ["name"],
          "properties": {
            "name": { "type": "string" }
          }
        }
      }
    }
  }
}
