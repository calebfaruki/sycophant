# Shared across all sycophant tenants. The airlock controller in each
# tenant namespace authenticates workspace SA tokens via TokenReview, a
# cluster-scoped API. The `tenant-rolebinding-generator.yaml` Kyverno
# policy creates a per-tenant ClusterRoleBinding pointing each tenant's
# airlock-ctrl SA at this ClusterRole when the tenant namespace appears.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: sycophant-airlock-tokenreview
  labels:
    {{- include "sycophant.labels" . | nindent 4 }}
    app.kubernetes.io/component: airlock-ctrl
rules:
  - apiGroups: ["authentication.k8s.io"]
    resources: ["tokenreviews"]
    verbs: ["create"]
