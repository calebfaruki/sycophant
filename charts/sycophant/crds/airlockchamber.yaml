apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: airlockchambers.airlock.dev
spec:
  group: airlock.dev
  names:
    categories: []
    kind: AirlockChamber
    plural: airlockchambers
    shortNames: []
    singular: airlockchamber
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workspace
      name: Workspace
      type: string
    - jsonPath: .spec.workspaceMode
      name: Mode
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.keepalive
      name: Keepalive
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AirlockChamberSpec via `CustomResource`
        properties:
          spec:
            properties:
              credentials:
                items:
                  properties:
                    env:
                      nullable: true
                      type: string
                    file:
                      nullable: true
                      type: string
                    secret:
                      type: string
                  required:
                  - secret
                  type: object
                type: array
              egress:
                items:
                  properties:
                    host:
                      type: string
                    port:
                      format: uint16
                      maximum: 65535.0
                      minimum: 0.0
                      type: integer
                  required:
                  - host
                  - port
                  type: object
                type: array
              image:
                nullable: true
                type: string
              keepalive:
                default: false
                type: boolean
              workspace:
                type: string
              workspaceMode:
                type: string
              workspaceMountPath:
                default: /workspace
                type: string
            required:
            - workspace
            - workspaceMode
            type: object
        required:
        - spec
        title: AirlockChamberValidated
        type: object
        x-kubernetes-validations:
        - rule: '!has(self.spec.credentials) || !(self.spec.credentials.size() > 0 && self.spec.keepalive == true)'
        - rule: '!has(self.spec.credentials) || self.spec.credentials.all(c, (has(c.env) && !has(c.file)) || (!has(c.env) && has(c.file)))'
    served: true
    storage: true
    subresources: {}
