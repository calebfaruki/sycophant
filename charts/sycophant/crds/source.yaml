apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: sources.sycophant.md
spec:
  group: sycophant.md
  names:
    categories: []
    kind: Source
    plural: sources
    shortNames:
      - mfs
    singular: source
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.kind
      name: Kind
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for SourceSpec via `CustomResource`
        properties:
          spec:
            properties:
              kind:
                enum:
                - HostPath
                type: string
              hostPath:
                properties:
                  path:
                    pattern: "^/.+"
                    type: string
                required:
                - path
                type: object
            required:
            - kind
            type: object
            x-kubernetes-validations:
            - rule: "self.kind == 'HostPath' ? has(self.hostPath) : true"
              message: "kind 'HostPath' requires the hostPath block"
          status:
            nullable: true
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        title: Source
        type: object
    served: true
    storage: true
    subresources:
      status: {}
