# Pull policy overrides for local images not yet public on GHCR.
# Remove this file once sycophant images are published.

transponder:
  pullPolicy: Never

workspaces:
  hello-world:
    pullPolicy: Never
  multi-agent:
    pullPolicy: Never
